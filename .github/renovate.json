{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "dependencyDashboard": true,
  "extends": [
    "helpers:pinGitHubActionDigests"
  ],
  "enabledManagers": [
    "gradle",
    "github-actions"
  ],
  "packageRules": [
    {
      "description": "The workflow JDK only runs Gradle; track LTS releases (21, 25) so non-LTS bumps don't break the Android SDK setup step.",
      "matchDepNames": ["java-jdk"],
      "allowedVersions": "/^(21|25)(\\..*)?$/"
    }
  ]
}
